server:
  port: 9094

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

spring:
  application:
    name: ORDER-SERVICE
  datasource:
    url: mongodb://localhost:27017/test
    host: localhost
    port: 27017
    database: test
    repositories:
      enabled: true
